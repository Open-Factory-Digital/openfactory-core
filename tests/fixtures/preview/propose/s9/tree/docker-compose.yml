# The product as the team runs it: the API talks to a managed database in the cloud.
services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgres://app:${DB_PASSWORD}@db.prod.example.com:5432/app
      CACHE_URL: redis://localhost:6379/0
      SEARCH_HOST: search.internal.example.com
      WEB_ORIGIN: http://web:3000

  web:
    image: ghcr.io/acme/web:latest
    ports:
      - "3000:3000"
    environment:
      API_URL: http://api:8000
