# Harbourline — web

The web front end a shipper uses: ask for a quote, accept it, book against it.

- `src/quote.ts` — asks the pricing service for a quote
- `src/booking.ts` — books against an accepted quote
- `assets/` — the pictures and the font; nothing to read
