# Harbourline — tracking

What is at sea: the events a sailing emits, and when it is expected to arrive (REQ-0003 in
`harbourline-context`).

- `tracking/events.py` — the events, by name
- `tracking/eta.py` — the expected arrival, and when a move of it is a delay worth telling
