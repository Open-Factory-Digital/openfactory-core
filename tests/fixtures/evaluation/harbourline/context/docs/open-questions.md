# Questions only the developers can answer

> Nothing waits for these answers. The factory works from the code; each question is asked again
> on the day a ticket touches the area — answer it then, or here, whichever comes first.

- Is the 6-hour threshold for a delay notice measured against the first expected arrival, or
  against the last one a shipper was told? (`tracking/eta.py`)
- Does a cancelled booking give its kilograms back to the sailing's capacity?
  (`harbourline/capacity.py`)
