# FastAPI service layer — foundation placeholder.
# TODO(slice-3): app with endpoints (create/list jobs) + GitHub webhook receiver.
# Kept as a seam so the CLI-first walking skeleton can graduate to a service
# without restructuring.
