"""The factory's own health — reported as tickets, never as chat lines.

Everything else in this package acts on a CLIENT's product. This one acts on the factory itself:
when the platform promises something and cannot deliver it, that fact becomes a ticket with an
owner, a state and a history, on a board that belongs to the deployment rather than to any client.
"""
